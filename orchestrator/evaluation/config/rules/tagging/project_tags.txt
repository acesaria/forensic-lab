# Project tagging file, layered on top of Plaso's tag_linux.txt (Phase 3.1
# Detector B). Plaso tagging syntax: a label followed by indented match lines.
# Behavioral only; no instance constants (enforced by the rule-leakage lint).

shell_history
  data_type is 'bash:history:command'
  data_type is 'shell:zsh:history'

cron
  data_type is 'syslog:cron:task_run'

log_deletion
  data_type is 'fs:stat' AND timestamp_desc contains 'deletion'

login
  data_type is 'syslog:line' AND body contains 'session opened'
