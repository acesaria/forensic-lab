# infra/ansible/vars/debian.yml

isf_base_packages:
  - git
  - golang-go


# Packages baked into the lab baseline. Scenario binaries are prepared on the
# builder VM; the victim never compiles or installs during a run.
baseline_present_pkgs:
  - build-essential
  - netcat-openbsd