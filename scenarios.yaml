# Thesis scenario registry. Code-backed scenarios need no configuration;
# Father temporarily remains on the legacy declarative path.
interactive_shell: {}
userland_father_ldpreload:
  scenario_yml: scenarios/userland_father_ldpreload/scenario.yml
