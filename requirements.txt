# atomic red team execution
atomic-operator
atomic-operator-runner
# paramiko is intentionally installed above the <3.0.0 pin declared by
# atomic-operator-runner; setup-venv.sh patches the metadata after install.
# The SSH client API used by atomic-operator-runner is unchanged in 3.x/5.x.
paramiko==2.11.0

# forensic orchestration
pyyaml>=6.0
pydantic>=1.10,<2.0
requests>=2.31
attrs>=23.0
cryptography>=40.0

libvirt-python